```bash
# Cài đặt pip
python install pip

# Cài đặt Django
pip install django

# Cài đặt phiên bản dj-static 0.0.6
pip install dj-static==0.0.6

# Cài đặt Pillow thông qua pip
python -m pip install pillow

# Cors disabled
python -m pip install django-cors-headers

# Tạo người dùng quản trị viên
python manage.py createsuperuser

# Tạo các tệp migration cho Django
python manage.py makemigrations

# Thực hiện migration
python manage.py migrate

# Khởi chạy máy chủ Django
python manage.py runserver

pip install django-allauth

pip install PyJWT

pip install cryptography

pip install Pyotp


